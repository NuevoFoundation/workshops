#include <iostream>
using namespace std;

int main() {

  // 1: Declare a string variable

  // 2: Use cin to input data from the console and stored it in the variable that was declared in the previous step

  // 3: Print to the console "Hello " and the content of the variable that was declared in the first step and populated in the previous step
	
  return 0;  
}