#include <iostream>
using namespace std;

int main() {
  //1. Create an integer variable.

  //2. Input via the console (hint use cin) the content of that variable

  //3. With the help of if/else check if the number input equals 1, if that is the case we will encrypt the message, otherwise the message should be decrypted

  //4. Print to the console in either case what action was requested, for instance "Encrypt a message" or "Decrypt a message"

  return 0;
}