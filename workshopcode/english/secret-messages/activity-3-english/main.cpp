#include <iostream>
using namespace std;
int main() {  
  
  string name;
  cin >> name;
  // cout << "Hello " << name << endl;

  //Use a for loop that will run starting at value 0, while i is smaller (<) than the size of the name variable

  //Within the for loop body, access the character at position i and print it to the console
	
}