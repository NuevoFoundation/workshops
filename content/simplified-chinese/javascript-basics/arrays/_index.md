```markdown
---
title: "数组"
date: 2020-07-28T11:45:38-07:00
draft: true
hidden: true
weight: 2
---

## 什么是数组？

数组就是一个值的集合。它是一种特殊的变量，可以同时保存多个值。

例如，考虑一个名为`fruits`的数组，它可以包含像苹果、橙子、芒果等值...所有这些值都存在于一个变量中。

## 如何创建数组

以下语句展示了两种创建数组的方法，但最终结果是相同的。

<b>语法：</b>
```javascript
var arrayName = new Array(element0, element1, ..., elementN);
var arrayName = Array(element0, element1, ..., elementN);
var arrayName = [element0, element1, ..., elementN];
```

<b>示例：</b>
![#找不到图片](../img/syntax.png)
```